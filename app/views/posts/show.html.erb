<div class="home-banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= @photos.first.cloud_id  %>);">

  <h2><%= @post.title %></h2>

  <!-- scrool down button -->
  <section id="section04" class="demo">
    <a href="#section05"><span></span>Scroll</a>
  </section>
</div>


<div class="card-container">
  <div class="cards">
    <% @photos.each do |photo|  %>
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url(<%= photo.cloud_id  %>)">
        </div>
    <% end %>
  </div>
</div>








